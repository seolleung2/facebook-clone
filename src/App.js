import React from "react";
import Header from "./Header";
import "./App.css";

function App() {
  return (
    // BEM naming convention
    <div className="App">
      {/* Header */}
      <Header />
      {/* App body */}
      {/* Sidebar (left side)*/}
      {/* Feed (in the Center) */}
      {/* Widgets (right side)*/}
    </div>
  );
}

export default App;
